# 브릿지의 남은 판단 로직(location/notify/speech/preflight)은 표준 라이브러리만 씁니다.
# carecall_drt(케어콜 분석+대화+DRT 백엔드 호출) 패키지는 httpx/pydantic/google-genai를
# 씁니다. 아래 버전 범위는 carecall_drt/requirements.txt·requirements-gemini.txt와도
# 호환되도록 맞춰져 있습니다.

# 실제 drt_service를 HTTP로 호출할 때 필요합니다(브릿지·carecall_drt 공용).
httpx>=0.28,<1.0

# 메인 서버(main_server/) — 통화를 받는 FastAPI 서버입니다.
fastapi>=0.116,<1.0
uvicorn[standard]>=0.35,<1.0
pydantic>=2.10,<3.0

# carecall_drt의 GeminiJointResponder(다솜이 응답 + DRT 의미분석 1회 호출)가 씁니다.
# 없어도 서버는 뜨고, 대화는 규칙 기반(RuleCareResponder)으로 대신합니다.
google-genai>=1.0
python-dotenv>=1.0,<2.0

# bridge/notify.py::ClawOpsSmsSender(실제 문자 발송)가 씁니다. 없거나 키가
# 없으면 RecordingSmsSender(기록만)로 자동 폴백합니다.
clawops>=0.40,<1.0
