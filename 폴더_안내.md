# 어디서 작업해야 하나

헷갈리기 쉬운 부분만 짧게 정리했습니다. 실행 방법은 [RUNBOOK.md](RUNBOOK.md)를 보세요.

## 작업하는 곳 = 이 폴더 하나

```
2026.08.04_DRT\          ← 여기서만 수정합니다
├─ main_server/          ⓪ 메인 서버 — 통화 소유·전체 지휘 (포트 8002)
├─ carecall_drt/         ① 케어콜 대화 분석 + 다중 턴 상태 + DRT 계획/예약 호출("두뇌")
├─ bridge/               위치·음성규칙·문자·조회링크검증(main_server가 재사용) +
│                          독립 데모 파이프라인(live_demo.py/run_handoff.py용, 그대로 둠)
├─ care_call_bot/        통화 동의(consent.py)·음성 입력(voice_io.py), GEMINI_KEY 보관
├─ drt_service/          ② 정류장·경로·예약        (포트 8001)
└─ mock_drt_server/      ③ 배차·차량추적·조회페이지 (포트 8000)
```

**2026-08-11에 케어콜 대화 분석·상태 관리가 `carecall_drt`로 이식됐습니다**
(팀원이 새로 통합해 전달한 패키지). 무엇을 대체했고 왜 그런지는
[docs/04_carecall_drt_이식.md](docs/04_carecall_drt_이식.md)를 보세요.

## 쓰지 않는 폴더 (보관용)

같은 이름의 폴더가 다른 곳에도 있지만 **모두 보관용**입니다. 각 폴더 안에
`__보관용_*.md` 안내를 넣어 두었습니다.

| 보관용 폴더 | 상태 |
|---|---|
| `2026.07.27_DRT 알고\drt_service` | 팀 저장소 마지막 커밋 상태. 통합 작업은 여기 없음 |
| `Downloads\mock-drt-server-main` | 2026-08-06 첫 업데이트 이전 버전 |
| `Downloads\mock-drt-server-main (1)` | 팀의 2026-08-06 첫 업데이트(정류장 전체 표시 등). 이미 이식 완료 |
| `Downloads\mock-drt-server-main (2)` | 팀의 2026-08-06 두 번째 업데이트(CORS·쿼리스트링 링크, `web/` 제거). 이미 이식 완료 |
| `Downloads\drt-tracking-main` | 팀이 새로 분리한 조회 화면 원본(Leaflet+OSM, GitHub Pages 배포용). `mock_drt_server\web\tracking`에 이식 완료(same-origin으로 고쳐서) |
| `Downloads\care-call-bot-main` | 원본. 코드는 작업본과 동일 |

**보관용 폴더를 고쳐도 아무 데도 반영되지 않습니다.** 지워도 되지만,
`mock-drt-server-main (1)`은 다음 팀 업데이트 때 비교 기준으로 쓸 수 있습니다.

## drt_service 사본이 둘인 이유

팀 git 저장소라 통합 작업공간으로 복사할 때 이력(`.git`)까지 함께 왔습니다.
둘 다 같은 원격을 가리키므로 **어느 쪽에서 커밋하든 팀 저장소로 올라갑니다.**
그래서 한쪽만 쓰기로 정했습니다 — **작업본은 이 폴더의 `drt_service/`** 입니다.

| | 원본 폴더 | 이 폴더 (작업본) |
|---|---|---|
| git 상태 | 깨끗함 (마지막 커밋 그대로) | 통합 변경 8개 파일 (미커밋) |
| 배차 서버 연동 | 없음 | 있음 |
| 커밋·푸시 | 하지 않음 | **여기서 합니다** |

## API 키는 두 군데만

| 키 | 넣는 곳 |
|---|---|
| TMAP | `drt_service\.env` 의 `TMAP_APP_KEY` |
| Gemini | `care_call_bot\.env` 의 `GEMINI_KEY` |

나머지(배차 서버 TMAP 키, 브릿지 릴레이 토큰, carecall_drt의
`DRT_BACKEND_URL`/`DRT_RELAY_TOKEN`/`DRT_BACKEND_ENABLED`)는 `py scripts\setup.py` 가
맞춰 줍니다. 한 번 넣으면 setup을 다시 돌려도 지워지지 않습니다.

```powershell
py scripts\setup.py --check
```
